import React from 'react';
import ReactDOM from 'react-dom/client';

export default class NavLink extends React.Component {

    render() {
        return (
            <div>
                <a href="#" className="btn btn-lg btn-dark">Link</a>
            </div>
        );
    }
}